services:
  - type: web
    name: p3-api
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: p3-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
databases:
  - name: p3-db
    plan: free
